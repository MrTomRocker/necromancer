# Screenshots

Drop the README screenshots here (PNG). The README references exactly these three:

| File | What to capture |
|---|---|
| `overview.png` | The hero shot — the Necromancer service with a couple of guarded devices, each showing its `*_status` entity (ideally a mix of `ok` / `escalated`). |
| `add_device.png` | The first config-flow step of **Add device** (the health-source / source-type selection). |
| `guard_entities.png` | A single guard's device page showing its four entities (`status`, `health`, `auto_recovery` switch, `recover` button). |

Keep them reasonably cropped and light-theme for consistency. Once added, the `<img>` tags in
`README.md` will resolve on GitHub.
