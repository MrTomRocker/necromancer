"""Helper modules for the Necromancer config flow (schemas + reactive selectors).

`config_flow.py` must remain a single file at the integration root (hassfest
enforces this), so the pure schema/selector helpers live here to keep that file
focused on the flow handler classes.
"""
